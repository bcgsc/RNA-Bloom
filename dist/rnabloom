#!/bin/bash

set -eo pipefail

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"

if [ -z ${JVM_OPTS} ]
then
    JVM_OPTS=''
fi

set -u

if [ $# -eq 0 ]
then
java ${JVM_OPTS} -jar ${DIR}/RNA-Bloom.jar --help    
else
java ${JVM_OPTS} -jar ${DIR}/RNA-Bloom.jar "$@"
fi
